{
  "buildCommand": "cd whiteline-landing && npm install && npm run build",
  "outputDirectory": "whiteline-landing/dist",
  "installCommand": "echo skip-root-install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
